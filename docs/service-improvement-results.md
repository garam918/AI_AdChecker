# 서비스 개선과 검증 기록 — 2026-09-15

## 반영한 개선

- 마케터·판매자를 기본 사용자로 설정하고 텍스트·URL·이미지 세 진입점, 한 번에 실행하는 실제 대표 예제를 제공했다. 상세 제품 설정은 접고 예정 기능은 주 동선에서 제외했다.
- 결과의 핵심 이슈를 먼저 보여주고, 규정·모델·호출 기록은 점진적으로 공개한다. 모바일 이슈 선택은 수정 영역으로 이동한다.
- 일반 문구 제안, 증빙이 필요한 템플릿, 직접 검토 지침을 나눴다. 일괄 수정은 검토 대상 주장 삭제이며, 수상·수치·증빙을 만들어 넣지 않는다. 조건부 제안은 자동 적용하지 않는다.
- 현재 초안의 실제 인용을 검증하고 겹치는 범위·반복 인용·이미 변경된 표현을 건너뛴다. 수정 전후와 재검사 후 남은 표현을 비교한다.
- URL도 같은 수정·재검사 흐름을 제공한다. URL에서 가져온 초안의 재검사는 텍스트만 검사하며 원격 사이트를 수정하지 않는다.
- 이미지 원본, OCR 문구, 시각 관찰을 대조한다. 수정 초안은 OCR 광고 문구만 포함한다. 시각 관찰은 광고 문구나 삭제 완료 항목으로 취급하지 않는다. 이미지 위치 좌표는 추정하지 않는다.
- 실제 결과·초안·메모를 이 브라우저에 명시적으로 최대 10개 저장하고 다시 열 수 있다. 서버 동기화와 이미지 원본 보관은 없다.
- 검토 종료까지의 시간을 기록하되, 가정 30분 대비 배수는 제거했다. 실제 사용자 실험 절차는 [가치 검증 문서](./value-metrics.md)에 명시했다.
- 요청 취소, 오래된 응답의 화면 덮어쓰기 방지, AI 공유 시간 예산, 부분 성공과 규칙 대체 표시를 추가했다. 대체 제공자의 엄격한 구조화 출력·안전성 거부 처리를 보완했다.
- AI 자유 문구를 이슈 유형으로 쓰던 문제를 수정해 규정 팩의 고정 코드를 유지한다. 출처 검증 실패는 계속 추가 검토로 낮춘다.

## 자동 검증

- 포맷 검사, 린트, TypeScript 검사, 43개 테스트 파일의 269개 테스트 통과.
- 운영 빌드와 규정 인덱스 검사 통과(17개 문서·50개 조항). 이 검사는 원본/인덱스 일치 검사이며 공식 법령 최신성 검증은 아니다.
- 기존 회귀 40개: 탐지 27/27, 출처 연결 27/27, 오탐 0/13. 규칙 설계와 겹치는 내부 회귀 성적이다.

## 실제 평가 기록

결과는 실행별 고유 파일에 보관했다. 데이터·실행 횟수를 선택적으로 삭제하지 않았다. 출처 연결은 해석의 적합성이 아닌 인용 구조 검증이다. 출처 적합성과 수정안 사실성의 전문가 검토는 미완료다.

### 새 합성 도전 사례 40개 — 첫 실행

동일 데이터 SHA-256: `051272497f79858ffc76c0a9fae24282a91d85162da2ed7199cc97a926a3c14a`

| 지표                     | 규칙 단독     | 실제 AI 체인 (규칙 대체 포함) |
| ------------------------ | ------------- | ----------------------------- |
| 결과 반환                | 40/40         | 40/40                         |
| 최종 AI 완료             | 미실행        | 18/40 (45.0%)                 |
| 기대 이슈 유형 모두 탐지 | 10/24 (41.7%) | 16/24 (66.7%)                 |
| 탐지 사례의 출처 연결    | 10/10         | 15/16 (93.8%)                 |
| SAFE 오탐                | 3/16 (18.8%)  | 3/16 (18.8%)                  |
| 처리 중앙값 / p95        | 0ms / 2ms     | 9.067초 / 37.022초            |

첫 실제 AI 실행은 유형 정규화 보완 전이다. `challenge-ga-03/04/06`은 위험 설명을 생성했으나 이슈 코드를 한국어 자유 문구로 반환해 자동 유형 채점에서 누락됐다. 이 버그를 고치고 동일 데이터로 재실행한다. 이후 성적은 도전셋을 본 뒤의 개발 평가이며 독립 홀드아웃 성적이 아니다.

- [규칙 결과](../outputs/evaluations/challenge-rules-2026-09-15T13-58-32-598Z-30d0f0dc.md)
- [첫 실제 AI 결과](../outputs/evaluations/challenge-live-2026-09-15T13-59-06-395Z-03b3ec61.md)
- 별도 `13-58-43` 실행은 로컬 네트워크 제한으로 AI 0/40이며 위 실제 AI 지표에 섞지 않았다. 실패 기록은 보존했다.

### 대표 데모 반복

[실제 실행 JSON](../outputs/evaluations/demo-2026-09-15T14-09-23-397Z-02c30476.json): SaaS 텍스트 5회, 일반 식품 텍스트 5회, URL 예제 1회, 이미지 예제 1회.

- 텍스트 결과 반환 10/10, 최종 AI 완료 5/10. 모든 텍스트 결과에서 예상 분류와 검증된 출처 이슈를 확인했다.
- 전체 결과 반환 12/12, 최종 AI 완료 6/12. 이미지 전체 AI 분석 31.548초, URL은 규칙 대체 24.987초였다.
- 규칙 대체를 AI 성공이라고 세지 않는다. 실행 스크립트는 AI 미완료를 실패로 평가해 종료 코드 1을 반환했다.
- 이 관측은 향후 100% 성공·30초 완료를 보장하지 않는다. 당시 할당량·시간 초과·형식 오류가 남아 있었다.

## 실제 화면 점검

- 데스크톱: 텍스트 대표 예제 → 주장 2개 삭제 → `AI 서비스` → 재검수 → 비교·검토 메모·시간 기록 → 저장 → 새로고침 → 복원 확인.
- 모바일 390×844: 입력 화면의 분석 버튼, 첫 결과 화면의 핵심 이슈, 메뉴 닫힘, 이슈 선택 후 수정 영역 이동 확인. 검증 후 크기 설정 복원.
- URL: 9개 전체 이슈 중 핵심 3개를 표시. 선택한 주장 하나 삭제 후 텍스트 재검수에서 8개가 남고 원격 페이지 미검증 안내를 표시했다.
- 이미지: 할당량 오류와 재시도 확인. 재시도에서 OCR 성공·최종 규칙 대체를 구분하고 실제 원본과 OCR·시각 관찰 대조 화면 확인.
- 위 브라우저 테스트 중 기록된 시간은 개발 점검 시간이며 사용자 연구 결과가 아니다.

## 남은 외부 의존성과 한계

1. `OPENAI_API_KEY`가 없어 다른 AI 제공자의 실계정 전환은 검증하지 못했다. 코드·모의 응답 테스트는 존재하지만 ‘연결 완료’라고 주장하지 않는다.
2. Vertex 할당량·과부하·시간 제한이 AI 완료율에 영향을 준다. 안정적인 시연에는 실제 대체 인증과 운영 부하 검증이 필요하다.
3. 새 문맥 사례에서 오탐·미탐이 확인됐다. 전문가 출처·수정안 검토, 새로운 독립 평가 데이터가 필요하다.
4. 실제 사용자 비교 실험은 아직 하지 않았다. 30분→30초 같은 효과 주장은 사용할 수 없다.
5. 법령 자동 갱신·전문/별표 전체 커버리지, 계정 기반 서버 기록, 동영상·YouTube, 이미지 위치 좌표는 이번 범위에 추가하지 않았다.

## 유형 정규화 후 개발 재평가 — 40개 완료

[최종 실행 결과](../outputs/evaluations/challenge-live-2026-09-15T14-14-37-437Z-39856aff.md) · [전체 분석·호출 기록](../outputs/evaluations/challenge-live-2026-09-15T14-14-37-437Z-39856aff.json)

| 지표                          | 관측값              |
| ----------------------------- | ------------------- |
| 결과 반환                     | 40/40               |
| 최종 AI 완료                  | 11/40 (27.5%)       |
| 규칙 대체                     | 29/40               |
| 기대 유형 모두 탐지           | 14/24 (58.3%)       |
| 탐지 사례의 출처 연결         | 13/14 (92.9%)       |
| SAFE 오탐                     | 5/16 (31.3%)        |
| 분류 일치                     | 39/40               |
| 처리 중앙값 / p95             | 10.314초 / 36.979초 |
| 이슈 있는 결과에서 핵심 1~3개 | 19/19               |

대체된 29건의 마지막 실패는 할당량 23건, 시간 초과 5건, 응답 형식 1건이었다. 따라서 유형 코드 버그를 고쳤다고 전체 탐지 품질이나 안정성이 향상됐다고 결론 내릴 수 없다. 이 실행의 AI 체인은 규칙 단독보다 더 많은 FLAG를 찾았지만 오탐도 늘었다. 계정 가용성과 표본에 따라 달라지는 두 실행을 모두 공개하며 높은 쪽만 대표 수치로 택하지 않는다.

구체적인 오탐:

- `challenge-ga-13`: 중립적인 문서 업로드·업무 배정 기능 설명에 실증 요구 이슈를 생성함 (AI).
- `challenge-ga-15`: 무료 기간·자동 결제 없음·가격을 밝혔는데도 조건 누락으로 지적함 (규칙 대체).
- `challenge-ga-19`: ‘최고라고 주장하지 않는다’는 부정문을 우월성 주장으로 탐지함 (규칙 대체).
- `challenge-food-15`: 건강기능식품이 아니라는 문구를 건강기능식품으로 분류하고 품목 검증을 요구함 (규칙 대체).
- `challenge-food-17`: 약을 대체하지 말라는 주의 문구를 의약품 오인 위험으로 지적함 (AI).

데이터는 같은 해시를 유지했다. JSON의 구현 해시는 실행 시작 시점의 `src`·`scripts` 스냅샷이다. 실행 중 타입 검사상의 중복 프로퍼티를 동등한 조건식으로 정리했고, 이후 화면의 OCR 초안·취소 탭 보완이 있었지만 이 텍스트 평가의 분석 판단 로직을 추가 조정하지 않았다.

## 운영 화면 검증

- 운영 대표 문구: Vertex `gemini-3.8-flash` 실제 2단계 성공, 총 22.4초, 핵심 이슈 2개. 대체 제공자는 ‘인증 미구성’으로 표시됨.
- 일반 문구 제안을 선택하고 최상급 표현을 삭제해 `업무 시간 단축을 돕는 AI 서비스` 초안 생성 확인. 수치·수상 정보를 추가하지 않았고 접미사가 중복되지 않았다.
- 위 초안 재검수에서 4.4초 후 이슈 0개를 반환했으나 `AI_RATE_LIMIT`에 따른 규칙 대체였다. LOW는 적법 보장이 아니며, 문맥 해석 미완료 안내를 확인했다.
- 최종 운영 버전: `1af8ce38-61c9-4251-8a39-d31ecb115996`, 배포 코드 커밋 `42f6af27ef863f27550b56d79389e5d92ef14390`. 기존 운영 주소를 갱신했고 다른 호스팅·계정·프로젝트는 변경하지 않았다.
- 취소 회귀: 이미지 탭을 사용한 뒤 대표 텍스트 분석을 시작하고 취소해도 광고 문구 탭과 원문으로 돌아오는 것을 확인했다. 이미 서버에서 시작한 AI 처리의 취소까지 보장하지는 않는다.
- 최종 운영 이미지 검사: 42.5초 후 이슈 2개 반환. OCR은 성공했으나 최종 AI 해석은 `AI_TIMEOUT`으로 규칙 대체. 실제 원본·OCR·시각 관찰을 대조했고 수정 초안에는 `[시각 관찰]` 메타데이터가 없음을 확인했다. 검사한 운영 탭에서 콘솔 오류·경고는 관측되지 않았다.
- 변경 파일별 별도 커밋으로 보관했으며 원격 Git 푸시는 하지 않았다. 생성된 상세 실행 파일은 Git에서 제외된 로컬 `outputs/evaluations/`에 있으며, 다른 기기에서 검토하려면 별도로 전달해야 한다.
